spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:C:/Users/UtkarshRathore/Documents/workspace/ms-project1/local-config-repo
          default-label: master
          #clone-on-start: true

server:
  port: 8888
